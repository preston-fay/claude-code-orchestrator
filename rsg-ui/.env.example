# RSG UI Environment Configuration

# Backend API URL (Railway deployment)
VITE_ORCHESTRATOR_API_URL=https://eloquent-liberation-production.up.railway.app

# Default user settings (used if not set via Settings panel)
VITE_DEFAULT_USER_ID=dev-user
VITE_DEFAULT_USER_EMAIL=dev@example.com
