version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: python3 -m uvicorn orchestrator_v2.api.server:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
